{
  "name": "forest-throne-phaser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit --allowJs --checkJs --skipLibCheck --lib ES2020,DOM --target ES2020 --module ESNext --moduleResolution bundler --esModuleInterop src/main.js",
    "export:data": "node ./scripts/export-data-csv.mjs"
  },
  "dependencies": {
    "phaser": "^3.70.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^5.4.0"
  }
}
